{
  "name": "bookmark-cleaner",
  "version": "1.0.0",
  "description": "A local web app that cleans, deduplicates, and re-classifies Chrome bookmark exports",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/**/*.test.js"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "express": "^5.2.1",
    "fastest-levenshtein": "^1.0.16",
    "multer": "^1.4.5-lts.1",
    "p-limit": "^7.3.0"
  }
}
