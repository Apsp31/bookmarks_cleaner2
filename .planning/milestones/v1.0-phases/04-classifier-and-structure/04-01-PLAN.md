---
phase: 04-classifier-and-structure
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/classifier.js
  - src/hierarchyBuilder.js
  - test/classifier.test.js
  - test/hierarchyBuilder.test.js
autonomous: true
requirements:
  - CLASS-01
  - CLASS-02
  - STRUCT-01
  - STRUCT-02

must_haves:
  truths:
    - "classifyByDomain returns correct category for known hostnames (github.com -> Development, youtube.com -> Video, reddit.com -> Community)"
    - "classifyByDomain strips www. prefix before lookup so www.github.com returns Development"
    - "classifyByDomain returns null for unknown domains and malformed URLs"
    - "classifyByMetadata scans OG title+description for category keywords and returns first match"
    - "classifyByMetadata returns null when metadata is undefined/null (HEAD-only responses)"
    - "classifyNode chains domain->metadata->Other fallback correctly"
    - "classifyTree deep-walks a tree and classifies all link nodes without mutating input"
    - "buildHierarchy groups links by category into folder nodes"
    - "buildHierarchy output never exceeds 3 levels deep (root -> category -> links)"
    - "buildHierarchy drops categories with 0 members (only creates folders for actual categories)"
    - "buildHierarchy with all links in one category produces exactly 1 top-level folder"
  artifacts:
    - path: "src/classifier.js"
      provides: "Domain rules map, OG keyword matching, classifyNode, classifyTree"
      exports: ["DOMAIN_RULES", "CATEGORY_KEYWORDS", "classifyByDomain", "classifyByMetadata", "classifyNode", "classifyTree"]
    - path: "src/hierarchyBuilder.js"
      provides: "Hierarchy construction from classified tree"
      exports: ["buildHierarchy"]
    - path: "test/classifier.test.js"
      provides: "Unit tests for classifier pipeline"
      min_lines: 60
    - path: "test/hierarchyBuilder.test.js"
      provides: "Unit tests for hierarchy builder"
      min_lines: 40
  key_links:
    - from: "src/classifier.js"
      to: "src/shared/types.js"
      via: "BookmarkNode typedef (category, metadata fields)"
      pattern: "node\\.category|node\\.metadata"
    - from: "src/hierarchyBuilder.js"
      to: "src/classifier.js"
      via: "Consumes classifyTree output (nodes with .category set)"
      pattern: "link\\.category"
---

<objective>
TDD: Build the classifier pipeline and hierarchy builder as pure, independently testable modules.

Purpose: Implement the core classification logic (domain rules map + OG metadata keyword matching) and the hierarchy construction (group by category into max-3-level tree). These are pure data transformations with clear I/O contracts -- ideal for TDD.

Output: `src/classifier.js`, `src/hierarchyBuilder.js`, and their test files. All tests green.
</objective>

<execution_context>
@/home/alan/code/claude/gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/alan/code/claude/gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-classifier-and-structure/04-CONTEXT.md
@.planning/phases/04-classifier-and-structure/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/shared/types.js:
```javascript
/**
 * @typedef {Object} BookmarkNode
 * @property {string} id - Stable UUID assigned at parse time (crypto.randomUUID())
 * @property {'folder'|'link'} type
 * @property {string} title
 * @property {string} [url] - Only present when type === 'link'
 * @property {number} [addDate] - Unix timestamp in seconds from ADD_DATE attribute
 * @property {BookmarkNode[]} [children] - Only present when type === 'folder'
 * @property {'ok'|'dead'|'uncertain'|'redirect'|'pending'} [linkStatus] - Set by Phase 3
 * @property {string} [category] - Set by Phase 4
 * @property {{title?: string, description?: string, image?: string}} [metadata] - OG/meta tags from page fetch
 * @property {string} [redirectUrl] - Original URL before redirect
 * @property {string} [finalUrl] - Final URL after redirect
 */
```

From test/dedup.test.js (test pattern):
```javascript
import { describe, it, beforeEach } from 'node:test';
import assert from 'node:assert/strict';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD classifier.js -- domain rules, OG metadata, and full pipeline</name>
  <files>test/classifier.test.js, src/classifier.js</files>
  <read_first>
    - src/shared/types.js (BookmarkNode typedef -- category and metadata fields)
    - src/linkChecker.js lines 48-64 (how metadata is populated: {title?, description?, image?})
    - test/dedup.test.js (test pattern: node:test + node:assert/strict imports, describe/it structure)
    - .planning/phases/04-classifier-and-structure/04-RESEARCH.md (Pattern 1-3 reference implementations)
  </read_first>
  <behavior>
    classifyByDomain tests:
    - classifyByDomain('https://github.com/foo') returns 'Development'
    - classifyByDomain('https://www.github.com/foo') returns 'Development' (strips www.)
    - classifyByDomain('https://youtube.com/watch?v=abc') returns 'Video'
    - classifyByDomain('https://www.youtube.com/watch?v=abc') returns 'Video' (www. variant)
    - classifyByDomain('https://reddit.com/r/programming') returns 'Social / Community'
    - classifyByDomain('https://obscure-unknown-site-12345.com') returns null
    - classifyByDomain('not-a-url') returns null (malformed URL)
    - classifyByDomain('') returns null (empty string)

    classifyByMetadata tests:
    - classifyByMetadata({title: 'GitHub Actions Tutorial', description: 'Learn CI/CD'}) returns 'Development' (matches 'github' keyword)
    - classifyByMetadata({title: 'Buy the best shoes', description: 'Shop now'}) returns 'Shopping'
    - classifyByMetadata({title: 'Something completely unrelated', description: 'Nothing matches'}) returns null
    - classifyByMetadata(undefined) returns null (absent metadata -- HEAD-only responses per D-02)
    - classifyByMetadata(null) returns null
    - classifyByMetadata({}) returns null (empty metadata object, no title or description)

    classifyNode tests:
    - Link node with known domain -> category from domain rules (domain takes priority over metadata per D-01)
    - Link node with unknown domain + metadata -> category from metadata (OG fallback per CLASS-02)
    - Link node with unknown domain + no metadata -> 'Other' (per D-06)
    - Folder node -> returned unchanged (type !== 'link')
    - classifyNode does NOT mutate input node (immutable-stage pattern per D-12)

    classifyTree tests:
    - Tree with 3 links: 2 known domains, 1 unknown -> all have .category set
    - Tree with nested folders -> all leaf link nodes classified recursively
    - Input tree is NOT mutated (original nodes retain undefined category)
  </behavior>
  <action>
    **RED phase:** Create `test/classifier.test.js` with all tests above. Import from `../src/classifier.js`. Use `node:test` describe/it pattern and `node:assert/strict`. Construct BookmarkNode test fixtures inline (do NOT create shared fixture files). Run tests -- they MUST fail (module not found).

    **GREEN phase:** Create `src/classifier.js` with these exports:
    1. `DOMAIN_RULES` -- plain JS object mapping hostname string to category string. Include ~50-100 entries covering common domains. Required entries (per CLASS-01 examples): `github.com -> Development`, `youtube.com -> Video`, `reddit.com -> Social / Community`. Fill remaining entries at Claude's discretion across all 10 categories from D-05: Development, Video, Social / Community, News, Shopping, Tools, Reference, Design, Finance, Learning.
    2. `CATEGORY_KEYWORDS` -- object mapping category string to array of keyword strings. Order entries so specific categories (Development, Video) come before generic categories (Tools, Reference) to prevent greedy matching (per Pitfall 5 in RESEARCH.md).
    3. `classifyByDomain(url)` -- extracts hostname via `new URL(url).hostname`, strips leading `www.` via `.replace(/^www\./, '')`, looks up in DOMAIN_RULES. Returns category string or null. Wraps `new URL()` in try/catch returning null for malformed URLs.
    4. `classifyByMetadata(metadata)` -- returns null if metadata is falsy. Concatenates `metadata.title` and `metadata.description` (filtering out falsy values), lowercases, checks each category's keywords via `.some(k => text.includes(k))`. Returns first match or null.
    5. `classifyNode(node)` -- returns node unchanged if `node.type !== 'link'`. Otherwise returns `{ ...node, category }` where category = `classifyByDomain(node.url) ?? classifyByMetadata(node.metadata) ?? 'Other'`. Does NOT mutate input.
    6. `classifyTree(node)` -- recursive. If link, returns `classifyNode(node)`. If folder, returns `{ ...node, children: (node.children ?? []).map(classifyTree) }`.

    Run tests -- all MUST pass.

    **REFACTOR phase (if needed):** Clean up keyword lists, add comments explaining keyword ordering rationale.
  </action>
  <verify>
    <automated>node --test test/classifier.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/classifier.test.js contains `import { classifyByDomain, classifyByMetadata, classifyNode, classifyTree } from '../src/classifier.js'`
    - test/classifier.test.js contains at least 14 `it(` calls covering all behaviors listed above
    - src/classifier.js contains `export const DOMAIN_RULES = {`
    - src/classifier.js contains `export const CATEGORY_KEYWORDS = {`
    - src/classifier.js contains `export function classifyByDomain(`
    - src/classifier.js contains `export function classifyByMetadata(`
    - src/classifier.js contains `export function classifyNode(`
    - src/classifier.js contains `export function classifyTree(`
    - src/classifier.js contains `hostname.replace(/^www\./, '')` or equivalent www-stripping logic
    - src/classifier.js contains `'github.com': 'Development'` (or `"github.com": "Development"`)
    - src/classifier.js contains `'youtube.com': 'Video'` (or `"youtube.com": "Video"`)
    - src/classifier.js contains `'reddit.com': 'Social / Community'` (or `"reddit.com": "Social / Community"`)
    - DOMAIN_RULES has at least 50 entries (keys)
    - `node --test test/classifier.test.js` exits with code 0
  </acceptance_criteria>
  <done>All classifier tests pass. classifyByDomain handles known domains (including www. variants) and returns null for unknown/malformed URLs. classifyByMetadata falls back to keyword matching on OG metadata. classifyNode chains domain->metadata->Other. classifyTree deep-walks without mutating input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD hierarchyBuilder.js -- group by category, enforce 3-level cap</name>
  <files>test/hierarchyBuilder.test.js, src/hierarchyBuilder.js</files>
  <read_first>
    - src/shared/types.js (BookmarkNode typedef)
    - src/classifier.js (just created in Task 1 -- to understand classifyTree output shape)
    - test/classifier.test.js (test pattern consistency)
    - .planning/phases/04-classifier-and-structure/04-RESEARCH.md (Pattern 4 reference implementation)
  </read_first>
  <behavior>
    buildHierarchy tests:
    - 3 links all category='Development' -> root has 1 child folder titled 'Development' containing 3 links
    - 5 links across 3 categories -> root has 3 child folders, each containing correct links
    - All links same category -> exactly 1 top-level folder (per STRUCT-02)
    - Output root node has type='folder' and title='root'
    - Output max depth is 3 (root=0 -> category folder=1 -> links=2) (per D-08)
    - All input links appear in output (no links lost during restructuring)
    - Empty categories are NOT present in output (per D-04)
    - 'Other' category only appears if at least one link has category='Other' (per D-06)
    - Single-bookmark categories produce a folder with 1 child (per D-09)
    - Category folder ids are valid UUIDs (use crypto.randomUUID())
    - Input tree is NOT mutated
  </behavior>
  <action>
    **RED phase:** Create `test/hierarchyBuilder.test.js` with all tests above. Import `{ buildHierarchy }` from `../src/hierarchyBuilder.js`. Construct classified BookmarkNode test fixtures inline -- each link node must have a `.category` field set (simulating output from `classifyTree`). Use `node:test` describe/it pattern. Run tests -- they MUST fail.

    **GREEN phase:** Create `src/hierarchyBuilder.js` with:
    1. Internal function `collectLinks(node)` -- recursively collects all link nodes from a tree. If `node.type === 'link'`, returns `[node]`. Otherwise returns `(node.children ?? []).flatMap(collectLinks)`.
    2. `export function buildHierarchy(classifiedTree)` --
       a. Call `collectLinks(classifiedTree)` to flatten all links.
       b. Group by `link.category ?? 'Other'` into a `Map<string, BookmarkNode[]>`.
       c. Create category folder nodes: `{ id: crypto.randomUUID(), type: 'folder', title: categoryName, children: categoryLinks }`. Iterate over the Map (not over a fixed category list -- this ensures empty categories are never created per D-04).
       d. Sort category folders alphabetically by title for deterministic output.
       e. Return root node: `{ id: crypto.randomUUID(), type: 'folder', title: 'root', children: categoryFolders }`.
       f. v1: No sub-categorisation within top-level categories. Links are direct children of category folders. This gives max depth = 3 (root -> category -> link) satisfying D-08. `buildSubCategories` from RESEARCH.md is a pass-through returning links flat.

    Run tests -- all MUST pass.

    **REFACTOR phase:** Add JSDoc comments. Verify no mutation of input tree nodes.
  </action>
  <verify>
    <automated>node --test test/hierarchyBuilder.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/hierarchyBuilder.test.js contains `import { buildHierarchy } from '../src/hierarchyBuilder.js'`
    - test/hierarchyBuilder.test.js contains at least 8 `it(` calls covering all behaviors listed above
    - src/hierarchyBuilder.js contains `export function buildHierarchy(`
    - src/hierarchyBuilder.js contains `crypto.randomUUID()` for folder id generation
    - src/hierarchyBuilder.js contains `type: 'folder'` (creates folder nodes)
    - src/hierarchyBuilder.js does NOT contain an import of CATEGORY_KEYWORDS or DOMAIN_RULES (hierarchy builder is independent of classifier internals)
    - `node --test test/hierarchyBuilder.test.js` exits with code 0
    - `node --test test/classifier.test.js test/hierarchyBuilder.test.js` exits with code 0 (both suites green together)
  </acceptance_criteria>
  <done>Hierarchy builder groups classified links into category folders, produces a valid BookmarkNode tree with max depth 3, drops empty categories, handles single-bookmark categories, and preserves all input links. All tests pass.</done>
</task>

</tasks>

<verification>
Run both test suites together:
```bash
node --test test/classifier.test.js test/hierarchyBuilder.test.js
```
All tests pass. No existing tests broken:
```bash
node --test test/**/*.test.js
```
</verification>

<success_criteria>
- classifier.js exports DOMAIN_RULES (~50-100 entries), CATEGORY_KEYWORDS, classifyByDomain, classifyByMetadata, classifyNode, classifyTree
- hierarchyBuilder.js exports buildHierarchy
- classifyByDomain handles www. prefix stripping (Pitfall 1)
- classifyByMetadata returns null for absent metadata (HEAD-only responses)
- buildHierarchy output max depth = 3 (hard constraint D-08)
- buildHierarchy only creates folders for categories with members (D-04)
- Neither module mutates input (immutable-stage pattern)
- All classifier and hierarchy builder tests pass
- Full test suite still green
</success_criteria>

<output>
After completion, create `.planning/phases/04-classifier-and-structure/04-01-SUMMARY.md`
</output>
